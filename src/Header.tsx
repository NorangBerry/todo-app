import './App.css';

function Header() {
  return (
	<header className="App-header" style={{height:"40px", width:"100%", backgroundColor:"rgba(0,0,0,.15)", position:"fixed" }}>
		Todo-APP
    </header>
  );
}

export default Header;
